# Rondo

This repository contains the initial Slice 0 foundation for Rondo.

## Scripts

- `pnpm install`
- `pnpm dev`
- `pnpm build`
- `pnpm test`

## Structure

- `apps/api` for the Fastify API
- `apps/web` for the Vite React app
- `packages/*` for shared workspace packages
